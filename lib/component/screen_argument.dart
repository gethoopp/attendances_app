// You can pass any object to the arguments parameter.
// In this example, create a class that contains both
// a customizable title and message.
class ScreenArguments {
  final int Id;
  final String token;

  ScreenArguments(this.Id, this.token);
}
